1. **Замените параметры в файле `config.ini`:**

    - Откройте файл `config.ini`.
    - Замените заполнитель значениями своих учетных данных Telegram API и название клиента.

2. Установите необходимые зависимости:

    pip install -r requirements.txt

3. Запустите бота:

    python main.py